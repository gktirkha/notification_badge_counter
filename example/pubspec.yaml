name: notification_badge_example
description: "Demonstrates how to use the notification_badge plugin."

publish_to: "none"

environment:
  sdk: ^3.11.5

dependencies:
  flutter:
    sdk: flutter

  notification_badge_counter:
    path: ../

  cupertino_icons: ^1.0.8

dev_dependencies:
  flutter_lints: ^6.0.0

flutter:
  uses-material-design: true
